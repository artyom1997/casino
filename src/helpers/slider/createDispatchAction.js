export default function createDispatchAcation(
  prev,
  width,
  blockCount,
  blockLength,
  direction,
  slidesToScroll
) {
  if (direction === "left") {
  } else {
  }

  let action = {};

  return action;
}
